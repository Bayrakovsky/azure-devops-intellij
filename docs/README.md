# docs

Screenshots referenced from the root `README.md` (captured in Rider with a TFVC project open):

- `commit-changes.png` — the TFVC check-in (Commit Changes) view: detected local changes, check-in
  checks, and the built-in diff.
- `manage-services.png` — the Manage TFVC Servers and Workspaces dialog: servers/organizations and
  their workspace mappings.

PNG, any reasonable resolution; GitHub scales them down in the README table.
